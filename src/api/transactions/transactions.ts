export enum TransactionTypes {
	DEBIT = "debit",
	CREDIT = "credit",
	WITHDRAWAL = "withdrawal",
	SEND = "send",
}

export enum TransactionStatus {
	COMPLETED = "completed",
	FAILED = "failed",
	PENDING = "pending",
	AWAITING = "awaiting",
	PROCESSING = "processing",
}
